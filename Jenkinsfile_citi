pipeline {
    agent none
    stages {
		
        stage('Install NodeJS dependencies') {
            when {
                branch 'admin_citi_uat'
            }
            agent {label 'master'}
            steps {
                dir('./SourceCode/Frontend/frontend_citib2b') {
					sh 'npm install'
                }
				dir('./SourceCode/Frontend/frontend_citib2b/server') {
					sh 'npm install'
                }
            }
        }
        stage('Build NodeJS') {
            when {
                branch 'admin_citi_uat'
            }
            agent {label 'master'}
            steps {
                dir('./SourceCode/Frontend/frontend_citib2b') {
					sh 'REACT_APP_STAGE=CITI_UAT PUBLIC_URL=/ node --max-old-space-size=4000 scripts/build.js'
					sh 'cp -r build server/'
                }
            }
        }
        stage('Deploy CITI_UAT') {
            when {
                branch 'admin_citi_uat'
            }
            agent {label 'master'}
            steps {
                input 'Deploy to CITI UAT?'
                milestone(1)
                sshPublisher(publishers: [sshPublisherDesc(configName: 'uat-cte-webserver-1', transfers: [sshTransfer(cleanRemote: false, excludes: '', execCommand: '''pwd; chmod +x /CITI_UAT/admin_portal/deploy_run_env.sh; sh /CITI_UAT/admin_portal/deploy_run_env.sh CITI_UAT;''', execTimeout: 30000, flatten: false, makeEmptyDirs: false, noDefaultExcludes: false, patternSeparator: '[, ]+', remoteDirectory: '/CITI_UAT/admin_portal', remoteDirectorySDF: false, removePrefix: 'SourceCode/Frontend/frontend_citib2b/server', sourceFiles: 'SourceCode/Frontend/frontend_citib2b/server/**/*')], usePromotionTimestamp: false, useWorkspaceInPromotion: false, verbose: true)])			
            }
            post {
		       always {
                    cleanWs()
               }
            }
        }
    }
}